# Database Configuration
# For local PostgreSQL: postgresql://user:password@localhost:5432/dbname
# For Docker PostgreSQL: postgresql://urja_user:urja_password@postgres:5432/urja
DATABASE_URL=postgresql://urja_user:urja_password@localhost:5432/urja

# Application Configuration
NODE_ENV=development
PORT=5000

# Email Configuration (Zoho SMTP)
# Get your credentials from https://mail.zoho.com
# Settings → Mail Accounts → SMTP Settings

# Zoho SMTP server hostname
SMTP_HOST=smtp.zoho.com

# SMTP port: 587 for TLS (recommended) or 465 for SSL
SMTP_PORT=587

# Your Zoho email address (sender)
SMTP_USER=admin@urjacore.in

# Your Zoho account password or app-specific password
# If you have 2FA enabled, generate an app-specific password at:
# https://accounts.zoho.com/home#security/application
SMTP_PASS=your_zoho_password_here

# Email address to receive contact form notifications
ADMIN_EMAIL=admin@urjacore.in
